<section class="hero is-info is-small">
  <!-- Hero head: will stick at the top -->
  <div class="hero-head">
    <nav class="navbar">
      <div class="container">
        <div class="navbar-brand">
          <h1 class="title">
            <%= link_to root_path, class: "navbar-item" do %>
              <img width="20" height="20" src="https://www.flaticon.com/svg/static/icons/svg/1046/1046857.svg" class="loaded">
              <span class="has-text-weight-bold is-size-4 ml-4">BeoKaKun's Food Log</span>
            <% end %>
          </h1>

          <span class="navbar-burger burger" data-target="navbarMenuHeroA">
            <span></span>
            <span></span>
            <span></span>
          </span>
        </div>
        <div id="navbarMenuHeroA" class="navbar-menu">
          <div class="navbar-end">
            <span class="navbar-item">
              <%= link_to "Create New Entry", new_entry_path, class: "button is-success mr-3" %>
              <%= link_to 'New Category', new_category_path, class: "button is-success" %>
            </span>
          </div>
        </div>
      </div>
    </nav>
  </div>

  <!-- Hero content: will be in the middle -->
  <div class="hero-body">
    <div class="container has-text-centered">
      <h1 class="title">
        <% if content_for(:title) %>
          <%= yield :title %>
        <%  else %>
          Food Log
        <% end %>
      </h1>
      <h2 class="subtitle">
        Subtitle - 😃 😃 😃
      </h2>
    </div>
  </div>
</section>